name: Stop Cotiza (Render)
on:
  schedule:
    - cron: "00 20 * * 1-5"  # 20:00 UTC (17:00 ART)
  workflow_dispatch: {}

jobs:
  stop:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS -X POST https://cotiza-api-q3v6.onrender.com/cotizaciones/detener
